{% extends "DSGAdministrationBundle::layout.html.twig" %}

{% block title %}DataSet Generator{% endblock %}

{% block h1 %}DataSet Generator{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-sm-12">
            <h2>Global stats :</h2>
            <dl class="dl-horizontal"><dt>Total Europeana:</dt><dd>{{ countTotalEuropeana|number_format(2, '.', ',') }}</dd></dl>
            <hr />
            <h2>Country stats :</h2>
            <dl class="dl-horizontal"><dt>Total Completeness:</dt><dd>{{ completenessCountTotal|number_format(2, '.', ',') }}</dd></dl>

            <table class="table table-bordered table-striped">
                <thead>
                <tr>
                    <th>#</th>
                    <th>Completeness</th>
                    <th>Number</th>
                    <th>% on completenesses</th>
                    <th>% on Europeana</th>
                </tr>
                </thead>
                <tbody>
                {% for completeness, count in completenesses %}
                    <tr>
                        <td>{{ loop.index }}</td>
                        <td>{{ completeness }}</td>
                        <td>{{ count|number_format(2, '.', ',') }}</td>
                        <td>{% if count > 0 %}{{ ((count*100)/completenessCountTotal)|round(2, 'floor') }}%{% else %}0%{% endif %}</td>
                        <td>{% if count > 0 %}{{ ((count*100)/countTotalEuropeana)|round(2, 'floor') }}%{% else %}0%{% endif %}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}

{% block javascript_sub %}
    {{ parent() }}
{% endblock %}